public class ForEachLoop {

    public static void main(String[] args) {
        String [] colors = {"Red", "Blue", "Orange", "Pink"};
        // colorName will take all the values from the array
        for (String colorName : colors)
        {
            System.out.println(colorName);
        }
    }
}
